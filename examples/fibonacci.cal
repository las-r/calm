;use io

// fibonacci example in calm
// by las-r

def u64 fib(u64 n) {
    if n <= 1 {
        return n
    }

    return fib(n - 1) + fib(n - 2)
}

def i32 main() {
    u64 n

    printf("Enter n: ")
    scanf("%llu", @n)

    printf("fib(%llu) = %llu\n", n, fib(n))

    return 0
}
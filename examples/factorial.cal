;use io

// factorial example in calm
// by las-r

def u64 fact(u64 n) {
    if n <= 1 {
        return 1
    }
    return n * fact(n - 1)
}

def i32 main() {
    u64 n

    printf("Enter n for n!: ")
    scanf("%lld", @n)

    printf("n! = %llu\n", fact(n))
    
    return 0
}
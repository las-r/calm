extc def i32 printf([u8] f, ...)
extc def i32 scanf([u8] f, ...)

def i32 factorial(i32 n) {
    if n <= 1 {
        return 1
    }
    return n * factorial(n - 1)
}

def i32 main() {
    i32 n
    printf("Enter n for n!: ")
    scanf("%d", &n)
    printf("n! = %d\n", factorial(n))
    return 0
}